console.log(['1', '2', '3'].map(parseInt))

// And parseInt is used to parse the string, making the string an integer with a specified base.
// parseInt(string, radix) receives two parameters,
// the first represents the value (string) to be processed,
// and the second represents the radix during parsing.
